{
  "name": "but-tc-skills",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "./start.sh",
    "deploy": "./deploy.sh",
    "dev:start": "docker-compose -f docker-compose.dev.yml up -d --build",
    "dev:stop": "docker-compose -f docker-compose.dev.yml down",
    "prod:backup": "./infrastructure/backup_production.sh",
    "prod:restore": "./infrastructure/restore_backup.sh",
    "prod:check-backup": "./infrastructure/check_backup_status.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
}
